{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Date Type",
  "description": "The nature of the date assosciated with the digital object",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique identifier for the vocabulary",
      "default": 5,
      "type": "integer"
    },
    "vocabulary": {
      "description": "Distinct dates for status events that can be assigned to publications and data.",
      "type": "string",
      "enum": ["accepted", "approved", "available", "copyrighted", "collected", "created", "issued", "modified", "posted", "published"]
    }
  },
  "required": ["id", "vocabulary"]
}
