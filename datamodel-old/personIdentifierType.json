{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Person Identifier Type",
  "description": "The type of identifier that has been used to reference a Person",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique identifier for the vocabulary",
      "default": 12,
      "type": "integer"
    },
    "vocabulary": {
      "description": "Incomplete list of identifier types that can be assigned to a Person.",
      "type": "string",
      "enum": ["ORCID", "FOAF", "Twitter", "eduPersonTargetedID", "eduPersonPrincipleName"]
    }
  },
  "required": ["id", "vocabulary"]
}
