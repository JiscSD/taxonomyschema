{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Preservation Event Type",
  "description": "Events that occur in preservation workflows, derived from Library of Congress vocabulary",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique identifier for the vocabulary",
      "default": 13,
      "type": "integer"
    },
    "vocabulary": {
      "description": "Actions performed on digital objects within a preservation repository.",
      "type": "string",
      "enum": ["capture", "compression", "creation", "deaccession", "decompression", "decryption", "deletion", "digitalSignatureValidation", "download", "fixityCheck", "ingestion", "messageDigestCalculation", "migration", "normalisation", "replication", "update", "validation", "virusCheck"]
    }
  },
  "required": ["id", "vocabulary"]
}
