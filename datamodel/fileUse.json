{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "File Use",
  "description": "Description of file used in preservation workflows",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique identifier for the vocabulary",
      "default": 8,
      "type": "integer"
    },
    "vocabulary": {
      "description": "File type described by function or role of the file in the preservation process.",
      "type": "string",
      "enum": ["originalFile", "thumbnailImage", "extractedText", "preservationMasterFile", "intermediateFile", "serviceFile", "transcript"]
    }
  },
  "required": ["id", "vocabulary"]
}
